<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!--背景图 -->
    <item android:id="@android:id/background" android:drawable="@android:color/white"/>

    <!--全部能量图 -->
    <item android:id="@android:id/secondaryProgress" android:drawable="@android:color/holo_blue_bright" />

    <!-- 进和能量图 -->
    <item android:id="@android:id/progress" android:drawable="@android:color/holo_green_light" />
</layer-list>